# Arcana pipeline configuration

[pipeline]
pair = "ETH-USD"
source = "coinbase"

[database]
host = "localhost"
port = 5432
database = "arcana"
user = "arcana"
password = "arcana"

# Bar construction is now handled by Sigil (separate package).
# See sigil.toml for bar configuration.
